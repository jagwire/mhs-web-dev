apply plugin: 'java'
apply plugin: 'application'

mainClassName = "Main"

sourceSets {
    main {
        java {
            srcDirs = ['src/classes']
        }
    }
}

repositories {
    
    mavenCentral()
}

dependencies {
    compile group: "com.sparkjava", name: "spark-core", version: "2.2"
}